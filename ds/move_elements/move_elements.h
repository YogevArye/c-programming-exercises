#ifndef  MOVE_ELEMENTS_H
#define  MOVE_ELEMENTS_H

#include "utils.h"

static void ShiftLeft(int array[], size_t size);
void MoveElements(int array[], size_t size, size_t count);
int CompareArrays(int arr1[], int arr2[], size_t size);
void RunTestCases();

#endif 